# Product Overview

ASP.NET MVC web application built with .NET Framework 4.8. The application uses the Model-View-Controller pattern for web request handling and includes Application Insights telemetry for monitoring.

Currently implements a basic controller structure with routing configured for standard MVC patterns.
